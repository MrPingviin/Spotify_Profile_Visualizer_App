const SpotilogoNoText = ({width, onClick}) => {
    return(
        <img width={width} onClick={onClick} src="./spotilogo_withouttext.svg" alt="Spotify logo." />
    )
}

export default SpotilogoNoText;
